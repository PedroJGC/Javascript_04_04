/*
    Classes
    - Método estatico
*/

class User {
  static showMessage(message) {
    console.log(message);
  }
}

// const user = new User('Essa é uma mensagem');
// user.showMessage();

User.showMessage('Essa é uma mensagem');
